export const metadata = {
  title: 'CROWN INTELLIGENCE — Strategic AI Systems & OpenClaw Deployment',
  description:
    'Crown Intelligence designs premium AI systems, OpenClaw deployments, and strategic operating infrastructure for companies that want leverage, clarity, and advantage.',
};

import './globals.css';

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
